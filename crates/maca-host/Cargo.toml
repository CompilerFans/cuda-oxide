[package]
name = "maca-host"
version = "0.1.0"
edition = "2021"
description = "Host-side kernel launch infrastructure for MXMACA backend"

[dependencies]
maca-bindings = { path = "../maca-bindings" }
maca-core = { path = "../maca-core" }
cuda-macros = { path = "../cuda-macros" }
anyhow = { workspace = true }
